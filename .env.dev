# Ambiente de desenvolvimento local
# Use com: docker compose --env-file .env-dev up --build

# Aplicação
APP_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Banco de Dados
POSTGRES_PORT=5432
POSTGRES_DB=controleestoque
POSTGRES_USER=postgres
POSTGRES_PASSWORD=root

# Segurança / JWT
JWT_SECRET=dev-secret
JWT_EXPIRE_MS=3600000

# CORS (separe com vírgula)
CORS_ORIGINS=http://localhost:8080,http://localhost:80
